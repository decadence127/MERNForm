{
  "name": "server",
  "version": "1.0.0",
  "description": "Fullstack sample app",
  "main": "app.js",
  "scripts": {
    "dev": "nodemon app.js",
    "start": "cross-env NODE_ENV=production node app.js",
    "client": "npm run start --prefix ../client",
    "client:install": "npm install --prefix ../client",
    "client:build": "npm run build --prefix ../client",
    "test": "cross-env NODE_ENV=development concurrently \"npm run dev\" \"npm run client\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^6.0.5"
  },
  "devDependencies": {
    "concurrently": "^6.2.1",
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.12"
  }
}
